version: 2
jobs:
  release:
    branches:
      only:
        - master
        - develop
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: npx semantic-release
workflows:
  version: 2
  ship_it:
    jobs:
      - release